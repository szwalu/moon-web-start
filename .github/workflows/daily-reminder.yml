name: Daily Journal Reminder

on:
  schedule:
    # 第一次：加州上午 09:12+8 (UTC 17:12)
    - cron: '12 17 * * *'
    # 第二次：加州晚上 20:12 (UTC 04:12)
    - cron: '12 4 * * *'
  # 允许手动触发
  workflow_dispatch:

jobs:
  send-notifications:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20' # 🔥 升级到 20 以消除警告

      # 注意：这里会生成 pnpm-lock.yaml，避免安装冲突
      - name: Install dependencies
        run: npm install -g pnpm && pnpm add firebase-admin @supabase/supabase-js

      - name: Run Reminder Script
        # 👇👇👇 核心修改：这几行 env 必须存在，且缩进正确 👇👇👇
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/daily-reminder.cjs
